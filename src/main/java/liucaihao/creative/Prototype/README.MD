原型模式用于创建重复对象，同时又能保证性能。这种模式是实现了一个原型接口，该接口用于创建当前对象的克隆。当创建新对象的代价比较大，或者数据准备特别繁琐，则可以采用原型模式。

克隆模式在java中实现Cloneable接口很容易实现,实现Cloneable只是浅复制，对于复杂对象（非基本对象），克隆只是复制了地址，克隆前后的两个对象的参数还是引用到同一个内存对象。

要实现复杂对象深复制需要复杂对象也实现Cloneable接口，并且在主对象中针对参数对象也调用clone接口设定克隆对象的值